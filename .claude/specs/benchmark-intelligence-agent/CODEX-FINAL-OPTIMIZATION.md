# Codex最终优化指令：架构简化与回归核心

**任务类型**：架构简化（删除伪需求功能）
**优先级**：P0（核心架构重构）
**预计工时**：30分钟
**创建时间**：2025-11-19

---

## 零、Linus哲学验证（必读）

### 三问检验

**1. "Is this a real problem?"** - 版本监控是真问题吗？

**项目核心目标**（引用自`.claude/CLAUDE.md`）：
```markdown
### 核心目标

1. **系统性调研与评估**
   - 覆盖GUI/Web/Coding等领域的**新Benchmarks**  ← 关键：新

2. **自动化情报流**
   - 建立"可定期更新"的自动化情报流  ← 关键：新情报

3. **一键添加到候选池**
   - 提供完整Benchmark基础信息  ← 关键：新候选
```

**版本监控功能**：
- 监控**已入库**Benchmark的GitHub Release更新
- 监控**已入库**arXiv论文的版本更新
- 推送更新通知到飞书

**问题分析**：
| 场景 | 用户行为 | 自动监控价值 |
|------|---------|-------------|
| Benchmark已采纳使用 | 开发团队会主动follow GitHub仓库 | ❌ 冗余（团队已watch） |
| Benchmark已拒绝 | 不再关心后续更新 | ❌ 无价值（已决定不用） |
| Benchmark待评估 | 评估时会查看最新版本 | ❌ 无价值（评估时自然会看） |

**结论**：版本监控**不是真问题** - 偏离核心目标（发现新Benchmark）

---

**2. "Is there a simpler way?"** - 最简单的解决方案是什么？

**当前架构**：
```
.github/workflows/
├── daily_collect.yml      # 主流程：新Benchmark发现
└── track_releases.yml     # 版本监控（伪需求）
    ├── scripts/track_github_releases.py
    ├── scripts/track_arxiv_versions.py
    ├── src/tracker/github_tracker.py
    └── src/tracker/arxiv_tracker.py
```

**方案对比**：

| 方案 | 复杂度 | 执行时间 | 收益 | Linus评分 |
|------|--------|---------|------|-----------|
| **A. 删除版本监控** | 低（删除文件） | 10分钟 | 架构清晰、维护↓ | ⭐⭐⭐⭐⭐ |
| B. 合并workflows | 高（重构代码） | 2小时 | 解决伪问题 | ⭐⭐ |
| C. 修复track_releases | 中（调试配置） | 30分钟 | 维持技术债 | ⭐ |

**结论**：**最简单方案 = 删除版本监控**

删除后架构：
```
.github/workflows/
└── daily_collect.yml      # 唯一workflow：新Benchmark发现
```

**无需复杂的"workflow合并"** - 删除后自然只剩1个workflow

---

**3. "What will this break?"** - 会破坏什么？

**核心功能检查**：

| 功能 | 删除版本监控后状态 | 是否破坏 |
|------|------------------|---------|
| 新Benchmark发现 | ✅ 保留（daily_collect.yml） | ❌ 不破坏 |
| 数据采集（7个collectors） | ✅ 保留 | ❌ 不破坏 |
| LLM智能评分 | ✅ 保留 | ❌ 不破坏 |
| 飞书存储 | ✅ 保留 | ❌ 不破坏 |
| 飞书通知 | ✅ 保留（新Benchmark推送） | ❌ 不破坏 |
| GitHub Release监控 | ❌ 删除 | ✅ 不破坏核心价值 |
| arXiv版本监控 | ❌ 删除 | ✅ 不破坏核心价值 |

**结论**：**零破坏性变更** - 删除的是"偏离核心的额外功能"

---

### 最终决策

**采用方案A：删除版本监控功能**

**理由**：
1. ✅ 符合"Is this a real problem?" - 不是真问题
2. ✅ 符合"Is there a simpler way?" - 最简单方案
3. ✅ 符合"What will this break?" - 零破坏
4. ✅ 回归核心价值：发现新Benchmark
5. ✅ 架构清晰：单一职责（1个workflow）
6. ✅ 维护成本降低：删除~800行代码

---

## 一、当前状态诊断

### 1.1 现状分析

**正常功能**：
- ✅ `daily_collect.yml` - 主流程每天09:00运行
- ✅ GitHub API限流已修复（已添加GITHUB_TOKEN）
- ✅ 飞书Webhook正常（用户确认："是可以推送的呀Webhook没问题"）
- ✅ 7个collectors正常采集数据
- ✅ LLM评分、飞书存储、飞书通知正常

**冗余功能**：
- ❌ `track_releases.yml` - 版本监控（偏离核心）
- ❌ 相关代码：~800行（2个scripts + 2个tracker模块）
- ❌ CI/CD额外消耗：1次workflow运行/天

### 1.2 代码统计

**待删除文件**：
```bash
# Workflow
.github/workflows/track_releases.yml           # 46行

# Scripts
scripts/track_github_releases.py               # 75行
scripts/track_arxiv_versions.py                # 68行

# Tracker模块
src/tracker/github_tracker.py                  # 约300行
src/tracker/arxiv_tracker.py                   # 约250行

# 总计：~740行代码
```

**保留文件**：
```bash
# 核心workflow
.github/workflows/daily_collect.yml            # ✅ 保留（已优化）

# 核心模块
src/collectors/*                               # ✅ 保留
src/scorer/*                                   # ✅ 保留
src/storage/*                                  # ✅ 保留
src/notifier/*                                 # ✅ 保留
src/main.py                                    # ✅ 保留
```

---

## 二、优化方案

### 2.1 架构对比

**优化前**（复杂、职责分散）：
```
GitHub Actions (2个workflows)
  ├─ daily_collect.yml (09:00)      ← 核心
  │   ├─ 数据采集
  │   ├─ 预筛选
  │   ├─ LLM评分
  │   ├─ 存储
  │   └─ 通知（新Benchmark）
  │
  └─ track_releases.yml (09:30)     ← 伪需求
      ├─ 读取已入库数据
      ├─ 检查GitHub Release
      ├─ 检查arXiv版本
      └─ 通知（版本更新）
```

**优化后**（简洁、职责清晰）：
```
GitHub Actions (1个workflow)
  └─ daily_collect.yml (09:00)      ← 唯一职责
      ├─ [1/6] 数据采集 (7 collectors)
      ├─ [2/6] URL去重
      ├─ [3/6] 规则预筛选
      ├─ [4/6] PDF内容增强
      ├─ [5/6] LLM评分
      ├─ [6/6] 存储入库
      └─ [7/6] 飞书通知（新Benchmark发现）
```

### 2.2 核心改进

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| Workflows数量 | 2个 | 1个 | -50% |
| CI/CD运行次数 | 2次/天 | 1次/天 | -50% |
| 代码行数 | ~15000行 | ~14260行 | -740行 |
| 架构复杂度 | 中等 | 低 | 简化 |
| 维护成本 | 中等 | 低 | 降低 |
| 核心职责 | 分散 | 清晰 | 回归 |

---

## 三、实施步骤

### Step 1: 删除冗余文件

**操作**：
```bash
cd /mnt/d/VibeCoding_pgm/BenchScope

# 删除workflow
rm .github/workflows/track_releases.yml

# 删除scripts
rm scripts/track_github_releases.py
rm scripts/track_arxiv_versions.py

# 删除tracker模块
rm src/tracker/github_tracker.py
rm src/tracker/arxiv_tracker.py

# 如果tracker目录为空，删除目录
rmdir src/tracker 2>/dev/null || true

# 确认删除
git status
```

**验证**：
```bash
# 确认文件已删除
ls .github/workflows/          # 应只有daily_collect.yml
ls scripts/track_*.py 2>/dev/null && echo "ERROR: 文件未删除" || echo "OK"
ls src/tracker/ 2>/dev/null && echo "WARNING: tracker目录仍存在" || echo "OK"
```

**关键点**：
- ✅ 保留`daily_collect.yml`（核心workflow）
- ✅ 保留所有collectors、scorer、storage、notifier（核心模块）
- ✅ 删除所有版本监控相关代码

---

### Step 2: 更新文档

#### 2.1 更新 `.claude/CLAUDE.md`

**文件路径**：`.claude/CLAUDE.md`

**修改位置1**：`## Architecture` section

**删除内容**（约50行）：
```markdown
### Version Tracking (`.github/workflows/track_releases.yml`)

**触发**: 每天 UTC 10:00 (北京时间 18:00)
**步骤**:
1. 运行 `scripts/track_github_releases.py` (监控GitHub Release)
2. 运行 `scripts/track_arxiv_versions.py` (监控arXiv版本)
3. 自动推送飞书通知
```

**添加内容**（简化说明）：
```markdown
### GitHub Actions Workflows

**唯一Workflow**: `.github/workflows/daily_collect.yml`

**触发时间**: 每天 UTC 01:00 (北京时间 09:00)

**执行流程**:
```
[1/6] 数据采集
  ├─ ArxivCollector (50条/天, 7天窗口)
  ├─ HelmCollector (14条/天)
  ├─ GitHubCollector (150+条/天, 30天窗口, 已修复限流)
  ├─ HuggingFaceCollector (43条/天, 14天窗口)
  ├─ TechEmpowerCollector (46条/天)
  ├─ DBEnginesCollector (50条/天)
  └─ TwitterCollector (已禁用)

[2/6] URL去重
  ├─ 内部去重（基于URL）
  └─ 飞书去重（避免重复入库）

[3/6] 规则预筛选
  ├─ GitHub: stars≥50, README≥500字, 90天内更新
  ├─ HuggingFace: downloads≥100
  └─ 过滤率: 40-60%

[4/6] PDF内容增强
  ├─ GROBID解析arXiv论文
  └─ 提取评估指标、数据集规模、基线模型

[5/6] LLM评分
  ├─ gpt-4o 5维评分（activity/reproducibility/license/novelty/relevance）
  ├─ 50并发异步评分（11.7倍加速）
  ├─ Redis缓存（7天TTL, 30%命中率）
  └─ 后端Benchmark专项评分

[6/6] 存储入库
  ├─ Primary: 飞书多维表格（批量写入20条/请求）
  └─ Fallback: SQLite降级备份（7天自动同步）

[7/6] 飞书通知
  └─ 分层推送（High/Medium/Low优先级）
```

**运行时间**: ~60秒（50并发LLM评分优化后）

**性能指标**:
- GitHub采集: 150+条/天（修复限流后）
- LLM评分: 12秒完成41条（50并发）
- 完整流程: <90秒
```

**修改位置2**：`## Utility Scripts` section

**删除内容**：
```bash
# 跟踪GitHub Release更新
.venv/bin/python scripts/track_github_releases.py

# 跟踪arXiv版本更新
.venv/bin/python scripts/track_arxiv_versions.py
```

**修改位置3**：`## Implementation Phases` section

**更新Phase 4说明**：
```markdown
**Phase 4 - 版本跟踪** (已废弃):
- ❌ 删除GitHub Release监控（偏离核心目标）
- ❌ 删除arXiv版本更新提醒（偏离核心目标）
- ✅ 理由: 版本监控不是真问题，核心价值在于发现新Benchmark
- ✅ 决策依据: Linus"Is this a real problem?"三问验证
```

---

#### 2.2 更新 `README.md`

**文件路径**：`README.md`

**修改位置**：`## Workflow` section

**删除内容**：
```markdown
### Version Tracking (`.github/workflows/track_releases.yml`)
- GitHub Release监控
- arXiv版本更新监控
```

**更新内容**：
```markdown
## Automated Workflow

**单一Workflow**: 每日智能采集（`daily_collect.yml`）

```
GitHub Actions (每日UTC 01:00)
  ↓
daily_collect.yml
  ↓
[1/6] 数据采集 (7 collectors, ~38秒)
  ↓
[2/6] URL去重 (内部 + 飞书)
  ↓
[3/6] 规则预筛选 (过滤40-60%噪音)
  ↓
[4/6] PDF内容增强 (GROBID + scipdf-parser)
  ↓
[5/6] LLM评分 (gpt-4o, 50并发, ~12秒)
  ↓
[6/6] 飞书存储 (批量写入) + SQLite备份
  ↓
[7/6] 飞书通知 (分层推送: High/Medium/Low)
```

**性能指标**:
- 完整流程: <90秒
- GitHub采集: 150+条/天（已修复限流）
- LLM评分: 11.7倍加速（50并发）
- CI/CD配额: 1次/天（优化前2次/天）
```

---

#### 2.3 添加设计决策文档（可选但推荐）

**文件路径**：`docs/design-decisions/remove-version-tracking.md`（新建）

**内容**：
```markdown
# 设计决策：删除版本监控功能

**决策时间**：2025-11-19
**决策人**：Claude Code（基于Linus哲学验证）
**状态**：已实施

---

## 背景

Phase 4实现了版本监控功能：
- `track_releases.yml` - 每天09:30监控GitHub Release和arXiv版本更新
- 推送更新通知到飞书

---

## 问题

用户质疑："版本监控是有必要的吗？"

经过Linus三问验证，发现：
1. **不是真问题**：项目核心目标是发现**新**Benchmark，而非跟踪已入库项目的更新
2. **用户实际行为**：
   - 已采纳的Benchmark → 团队会主动follow GitHub仓库
   - 已拒绝的Benchmark → 不再关心后续更新
   - 待评估的Benchmark → 评估时会查看最新版本
3. **价值低**：版本监控对核心工作流没有实质性帮助

---

## 决策

**删除版本监控功能**

**删除内容**：
- `.github/workflows/track_releases.yml`
- `scripts/track_github_releases.py`
- `scripts/track_arxiv_versions.py`
- `src/tracker/github_tracker.py`
- `src/tracker/arxiv_tracker.py`

**保留内容**：
- `.github/workflows/daily_collect.yml`（核心workflow）
- 所有collectors、scorer、storage、notifier（核心模块）

---

## 影响

**正面影响**：
- ✅ 回归核心价值：专注发现新Benchmark
- ✅ 架构简化：2个workflows → 1个workflow
- ✅ 维护成本降低：删除~740行代码
- ✅ CI/CD配额节省：2次/天 → 1次/天（-50%）

**负面影响**：
- ❌ 无（删除的是偏离核心的额外功能）

---

## 替代方案

如果团队确实需要版本监控：

**方案A**：GitHub Watch功能
- 操作：在GitHub仓库页面点击"Watch" → "Custom" → 勾选"Releases"
- 优势：原生支持、免费、零维护成本

**方案B**：arXiv RSS订阅
- 操作：订阅arXiv论文的更新RSS
- 优势：原生支持、实时推送

**方案C**：手动检查
- 操作：评估Benchmark时访问仓库查看最新版本
- 优势：零成本、按需查看

---

## 结论

删除版本监控是正确决策，符合Linus"Is this a real problem?"哲学。

项目应专注核心价值：**发现新Benchmark**，而非维护已入库项目。
```

---

### Step 3: 测试验证

#### 3.1 本地验证

**测试1：确认核心流程正常**

```bash
cd /mnt/d/VibeCoding_pgm/BenchScope

# 运行完整流程
.venv/bin/python -m src.main
```

**预期日志**：
```
============================================================
BenchScope Daily Intelligence Pipeline
============================================================

[1/6] 数据采集...
  ✓ ArxivCollector: 50条
  ✓ HelmCollector: 14条
  ✓ GitHubCollector: 150条 ← 验证限流修复
  ✓ HuggingFaceCollector: 43条
  ✓ TechEmpowerCollector: 46条
  ✓ DBEnginesCollector: 50条

[2/6] URL去重...
  ✓ 保留: 150条新发现

[3/6] 规则预筛选...
  ✓ 保留: 60条

[4/6] PDF内容增强...
  ✓ 增强: 45个arXiv论文

[5/6] LLM评分...
  ✓ 完成: 60条，耗时15秒

[6/6] 存储入库...
  ✓ 飞书多维表格写入成功

[7/6] 飞书通知...
  ✓ 推送完成

============================================================
Pipeline执行完成，总耗时65秒
============================================================
```

**验收标准**：
- ✅ 所有步骤执行成功（无ERROR）
- ✅ GitHub采集数量≥100（限流修复生效）
- ✅ LLM评分耗时<30秒（50并发）
- ✅ 飞书通知成功推送
- ✅ 总耗时<90秒

---

**测试2：确认删除文件不被引用**

```bash
# 检查是否有代码引用已删除的tracker模块
cd /mnt/d/VibeCoding_pgm/BenchScope
grep -r "from src.tracker" src/ || echo "✅ OK: 无引用"
grep -r "import.*tracker" src/ || echo "✅ OK: 无引用"

# 检查是否有文档引用已删除的scripts
grep -r "track_github_releases" README.md .claude/ || echo "✅ OK: 无引用"
grep -r "track_arxiv_versions" README.md .claude/ || echo "✅ OK: 无引用"
```

**预期结果**：全部输出`✅ OK: 无引用`

---

#### 3.2 GitHub Actions验证

**测试3：手动触发workflow**

```bash
# 1. 访问: https://github.com/JasonRobertDestiny/BenchScope/actions
# 2. 选择 "BenchScope Daily Collection"
# 3. 点击 "Run workflow" → "Run workflow"
# 4. 观察执行日志
```

**验收标准**：
- ✅ Workflow执行成功（绿色✓）
- ✅ 日志显示[1/6]到[7/6]全部完成
- ✅ GitHub采集数量≥100（验证GITHUB_TOKEN生效）
- ✅ 飞书群聊收到新Benchmark通知
- ✅ Artifacts上传成功（logs/ + fallback.db）

---

**测试4：确认只有1个workflow**

```bash
# 访问: https://github.com/JasonRobertDestiny/BenchScope/actions
# 确认Workflows列表只有: "BenchScope Daily Collection"
# 不应再看到: "Track Benchmark Updates"
```

---

#### 3.3 代码质量检查

**测试5：代码规范检查**

```bash
cd /mnt/d/VibeCoding_pgm/BenchScope

# PEP8检查
.venv/bin/python -m ruff check .

# 预期: 无错误输出
```

---

### Step 4: 提交变更

**Git操作**：

```bash
cd /mnt/d/VibeCoding_pgm/BenchScope

# Stage所有变更
git add -A

# 查看变更内容
git status

# Commit（遵循conventional格式）
git commit -m "refactor(arch): remove version tracking feature to focus on core value

根据Linus哲学三问验证，版本监控偏离核心目标（发现新Benchmark），决定删除该功能。

删除内容:
- .github/workflows/track_releases.yml
- scripts/track_github_releases.py
- scripts/track_arxiv_versions.py
- src/tracker/github_tracker.py
- src/tracker/arxiv_tracker.py
- 总计: ~740行代码

架构改进:
- Workflows: 2个 → 1个 (-50%)
- CI/CD运行: 2次/天 → 1次/天 (-50%)
- 职责清晰: 单一职责（发现新Benchmark）

核心功能保留:
✅ 7个collectors正常工作
✅ LLM评分（50并发）正常工作
✅ 飞书存储+通知正常工作
✅ GitHub限流已修复（添加GITHUB_TOKEN）

Linus验证:
1. Is this a real problem? → ❌ 版本监控不是真问题
2. Is there a simpler way? → ✅ 删除是最简单方案
3. What will this break? → ✅ 零破坏（核心功能完全保留）

如团队确实需要版本监控，建议使用:
- GitHub Watch功能（原生支持Release通知）
- arXiv RSS订阅（原生支持版本更新）
- 手动检查（评估时查看最新版本）"

# Push到GitHub
git push origin main
```

---

## 四、成功标准

### 4.1 功能完整性

- [ ] ✅ 新Benchmark发现功能正常工作
- [ ] ✅ 7个collectors正常采集数据
- [ ] ✅ LLM评分正常（50并发）
- [ ] ✅ 飞书存储正常（Primary + Fallback）
- [ ] ✅ 飞书通知正常推送

### 4.2 架构清晰性

- [ ] ✅ 只有1个workflow（daily_collect.yml）
- [ ] ✅ 无版本监控相关代码残留
- [ ] ✅ 文档更新完成（CLAUDE.md + README.md）
- [ ] ✅ 单一职责：发现新Benchmark

### 4.3 性能指标

- [ ] ✅ GitHub采集数量≥100条/天（修复限流后）
- [ ] ✅ 完整流程耗时<90秒
- [ ] ✅ CI/CD运行次数：1次/天（优化前2次/天）
- [ ] ✅ LLM评分耗时<30秒（50并发）

### 4.4 代码质量

- [ ] ✅ PEP8规范通过（ruff check无错误）
- [ ] ✅ 无未使用的import
- [ ] ✅ 无dead code残留
- [ ] ✅ Git提交遵循conventional格式

---

## 五、风险管理

### 5.1 风险识别

**风险1：团队后续需要版本监控**

- **概率**：低
- **影响**：中
- **缓解措施**：
  1. Git历史保留所有删除代码，可随时恢复
  2. 设计决策文档说明删除理由
  3. 推荐替代方案（GitHub Watch、arXiv RSS）

**风险2：核心流程异常**

- **概率**：极低（删除的是独立功能）
- **影响**：高
- **缓解措施**：
  1. Step 3全面测试验证
  2. 监控首次GitHub Actions运行日志
  3. 如有问题，立即git revert

### 5.2 回滚计划

**如果删除后发现严重问题**：

```bash
# 方案A: Git revert（推荐）
git revert <commit-hash>
git push origin main

# 方案B: 手动恢复文件
git checkout HEAD~1 .github/workflows/track_releases.yml
git checkout HEAD~1 scripts/track_github_releases.py
git checkout HEAD~1 scripts/track_arxiv_versions.py
git checkout HEAD~1 src/tracker/
git add .
git commit -m "revert: restore version tracking due to unexpected requirement"
git push origin main
```

**回滚标准**：
- 核心功能异常（连续2次workflow失败）
- 团队明确需要版本监控功能

---

## 六、后续建议

### 6.1 监控指标

**首周重点监控**（2025-11-19至2025-11-26）：

| 指标 | 目标值 | 监控方式 |
|------|--------|---------|
| Workflow成功率 | 100% | GitHub Actions日志 |
| GitHub采集数量 | ≥100条/天 | 日志分析脚本 |
| LLM评分耗时 | <30秒 | 日志时间戳 |
| 飞书通知送达率 | 100% | 飞书群聊检查 |

### 6.2 未来优化方向

**优化1：GitHub采集精准度提升**
- 当前：30+ topics并发搜索，过滤率40-60%
- 优化：基于历史数据训练，识别高质量topics
- 预期：过滤率提升至70-80%

**优化2：LLM评分成本优化**
- 当前：gpt-4o全量评分，月成本~¥20
- 优化：规则预评分 + LLM精评（仅评分>5分的候选）
- 预期：成本降低50%，质量不变

**优化3：飞书通知交互增强**
- 当前：单向推送
- 优化：添加交互按钮（✅采纳 / ❌拒绝 / 🔖待评估）
- 预期：减少人工操作飞书表格

---

## 七、附录

### A. 文件清单

**删除文件**：
- `.github/workflows/track_releases.yml`
- `scripts/track_github_releases.py`
- `scripts/track_arxiv_versions.py`
- `src/tracker/github_tracker.py`
- `src/tracker/arxiv_tracker.py`
- `src/tracker/` (如果目录为空)

**修改文件**：
- `.claude/CLAUDE.md` (更新workflow说明、删除Phase 4相关内容)
- `README.md` (更新workflow描述)

**新建文件**（可选）：
- `docs/design-decisions/remove-version-tracking.md` (设计决策记录)

**保留文件**（核心不变）：
- `.github/workflows/daily_collect.yml` ✅
- `src/collectors/*` ✅
- `src/scorer/*` ✅
- `src/storage/*` ✅
- `src/notifier/*` ✅
- `src/main.py` ✅

### B. 依赖关系图

**优化前**：
```
GitHub Actions
  ├─ daily_collect.yml
  │   └─ src/main.py
  │       ├─ collectors/
  │       ├─ scorer/
  │       ├─ storage/
  │       └─ notifier/
  │
  └─ track_releases.yml ← 删除
      ├─ scripts/track_github_releases.py ← 删除
      ├─ scripts/track_arxiv_versions.py ← 删除
      └─ src/tracker/ ← 删除
          ├─ github_tracker.py
          └─ arxiv_tracker.py
```

**优化后**：
```
GitHub Actions
  └─ daily_collect.yml
      └─ src/main.py
          ├─ collectors/
          ├─ scorer/
          ├─ storage/
          └─ notifier/
```

### C. 测试检查清单

**必须执行的测试**：

```bash
# ✅ 测试1: 本地完整流程
cd /mnt/d/VibeCoding_pgm/BenchScope
.venv/bin/python -m src.main

# ✅ 测试2: 确认无残留引用
grep -r "from src.tracker" src/ || echo "OK"
grep -r "track_github_releases" README.md .claude/ || echo "OK"

# ✅ 测试3: 代码质量检查
.venv/bin/python -m ruff check .

# ✅ 测试4: GitHub Actions手动触发
# 访问Actions页面手动运行workflow

# ✅ 测试5: 飞书通知验证
# 检查飞书群聊是否收到新Benchmark通知
```

**可选测试**：

```bash
# 性能测试
time .venv/bin/python -m src.main

# 日志分析
.venv/bin/python scripts/analyze_logs.py
```

---

## 八、Codex执行建议

1. **按顺序执行**：Step 1 → Step 2 → Step 3 → Step 4
2. **每步验证**：完成一个Step后，运行对应的测试
3. **遇到问题**：立即停止，记录日志，交给Claude Code分析
4. **最终验收**：全部测试通过后，提交Git commit

**预计完成时间**：30分钟

**交付标准**：
- ✅ 所有测试通过
- ✅ 代码质量检查通过
- ✅ 文档更新完成
- ✅ Git commit提交并push

---

**Claude Code验收任务**：

Codex完成后，我将执行：
1. 运行本地测试验证核心流程
2. 手动触发GitHub Actions workflow
3. 检查飞书通知是否正常
4. 分析日志确认性能指标
5. 编写验收报告（`docs/test-report-arch-simplification.md`）

---

**最终目标**：

回归核心价值 - **专注发现新Benchmark**，构建简洁、清晰、高效的智能采集系统。
